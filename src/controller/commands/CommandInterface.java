package controller.commands;

/**
 * Interface for all the commands which holds the method to execute
 * the action.
 */
public interface CommandInterface {

  /**
   * Method to execute the given command.
   */
  void run();
}
